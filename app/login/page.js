'use client'
import {useState} from 'react'
import {supabase} from '../../lib/supabase'
export default function Login(){const [form,setForm]=useState({email:'',password:''});const [msg,setMsg]=useState('');const [loading,setLoading]=useState(false)
 const submit=async(e)=>{e.preventDefault();setLoading(true);setMsg('');const {data,error}=await supabase.auth.signInWithPassword(form);setLoading(false);if(error)return setMsg(error.message);if(data.user&&!data.user.email_confirmed_at)return setMsg('Confirma tu correo electrónico antes de continuar.');window.location.href='/dashboard'}
 return <main className="shell"><header className="topbar"><a className="brand" href="/">Cotiza<span>Ya</span></a></header><section className="main"><form className="form" onSubmit={submit}><h1>Iniciar sesión</h1><p className="muted">Accede a tu negocio y a tus cotizaciones.</p>{msg&&<div className="notice">{msg}</div>}<div className="field"><label>Correo electrónico</label><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="correo@ejemplo.com"/></div><div className="field"><label>Contraseña</label><input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Tu contraseña"/></div><button className="btn" disabled={loading}>{loading?'Entrando…':'Iniciar sesión'}</button><p><a href="/recuperar">¿Olvidaste tu contraseña?</a></p><p className="muted">¿No tienes cuenta? <a href="/registro">Crear cuenta</a></p></form></section></main>}