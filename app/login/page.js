'use client'
import {useState} from 'react'
export default function Login(){const [msg,setMsg]=useState('');return <main className="shell"><header className="topbar"><a className="brand" href="/">Cotiza<span>Ya</span></a></header><section className="main"><div className="form"><h1>Iniciar sesión</h1><p className="muted">Accede a tu negocio y a tus cotizaciones.</p>{msg&&<div className="notice">{msg}</div>}<div className="field"><label>Correo electrónico</label><input type="email" placeholder="correo@ejemplo.com"/></div><div className="field"><label>Contraseña</label><input type="password" placeholder="Tu contraseña"/></div><button className="btn" onClick={()=>setMsg('La autenticación con Supabase será conectada en el siguiente paso.')}>Entrar</button><p><a href="/recuperar">¿Olvidaste tu contraseña?</a></p><p className="muted">¿No tienes cuenta? <a href="/registro">Regístrate</a></p></div></section></main>}